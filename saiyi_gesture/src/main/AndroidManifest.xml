<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lisijun.gesture">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <!--添加手势密码界面-->
        <activity android:name=".activity.AddGesturePassActivity"/>
        <!--验证手势密码界面-->
        <activity android:name=".activity.VerifyGesturePassActivity" />
    </application>

</manifest>
